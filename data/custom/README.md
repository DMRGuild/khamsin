Optional import inputs: index.html, about.html, main-header.html, main-footer.html, sidebar-header.html, sidebar-footer.html. These are trusted operator HTML. Preview with `npm run config:import -- --target sqlite --from ./data` and add `--apply` to merge. Existing slots are preserved unless `--overwrite-html` is specified. Normal operation edits slots in `/admin` and does not require these files.
